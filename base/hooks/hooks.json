{
  "description": "Plugin skill load mandate + convey high-yield-communication mandate. Both fire on SessionStart only — per-prompt re-injection wastes tokens; the discipline persists across the session.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/plugin-skill-mandate.sh SessionStart"
          },
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/convey-mandate.sh SessionStart"
          }
        ]
      }
    ]
  }
}
