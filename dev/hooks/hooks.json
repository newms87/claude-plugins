{
  "description": "Ideal-solution-mindset mandate (SessionStart) — surfaces the four core dev principles. DX-3235: the per-turn debugging-trigger gate (UserPromptSubmit, DX-3331: moved here from the retired `investigate` plugin) is removed — a per-turn gate can't tell a background task notification from an operator prompt (R-12); dev:debugging's own skill description now carries the load trigger instead.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/ideal-solution-mandate.sh SessionStart"
          }
        ]
      }
    ]
  }
}
