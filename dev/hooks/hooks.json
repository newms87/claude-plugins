{
  "description": "Ideal-solution-mindset mandate (SessionStart only). Surfaces the four core dev principles + self-trigger gates for debugging / testing / git-discipline so the agent loads the full skill at the right moment.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/scripts/ideal-solution-mandate.sh SessionStart"
          }
        ]
      }
    ]
  }
}
